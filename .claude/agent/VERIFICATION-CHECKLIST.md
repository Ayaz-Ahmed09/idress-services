BUILD & FUNCTION:
□ `npm run build` succeeds without errors
□ `npm run start` serves all pages
□ All 6 service routes load correctly
□ Homepage loads and all links work
□ Service selector navigates correctly
□ Sub-service WhatsApp buttons generate correct links
□ Booking form submits and sends email
□ Thank you page shows after submission
□ All phone call buttons have correct tel: links
□ Mobile menu opens/closes correctly
□ Floating CTA buttons visible on all pages
□ 404 page works for invalid routes

SEO:
□ Each page has unique <title> and <meta description>
□ Only ONE <h1> per page
□ Proper heading hierarchy
□ JSON-LD LocalBusiness present on all pages
□ JSON-LD Service schema on each service page
□ JSON-LD FAQ schema on each service page
□ JSON-LD Breadcrumb on service pages
□ Canonical URLs set correctly
□ Open Graph tags present
□ sitemap.xml accessible
□ robots.txt accessible
□ Semantic HTML used throughout

PERFORMANCE:
□ Lighthouse Performance score > 90
□ First Contentful Paint < 1.5s
□ Largest Contentful Paint < 2.5s
□ Total Blocking Time < 200ms
□ Cumulative Layout Shift < 0.1
□ No render-blocking resources
□ Images optimized (next/image)
□ Fonts optimized (next/font)
□ Minimal client-side JavaScript
□ No unused CSS/JS in bundle

RESPONSIVE:
□ All pages work on 320px width
□ All pages work on 375px (iPhone SE)
□ All pages work on 768px (tablet)
□ All pages work on 1024px (laptop)
□ All pages work on 1440px (desktop)
□ No horizontal overflow on any viewport
□ Touch targets ≥ 44px on mobile
□ Text readable without zoom

CONVERSION:
□ Phone number clickable on mobile
□ WhatsApp opens with pre-filled message
□ Booking form has proper validation
□ Multiple CTAs visible above fold
□ Trust signals visible (ratings, warranty, badges)
□ Clear pricing information shown
□ Emergency availability highlighted